from setuptools import setup, find_packages

setup(
    name='an-autonomic-framework-for-iot-data-confidentiality',
    version='0.0.0',
    packages=find_packages(exclude=['tests*']),
    install_requires=[],
    include_package_data=True
)